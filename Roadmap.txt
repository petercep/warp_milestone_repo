Project Roadmap: Geotechnical Structural Geology Workstation

1) Goal
Build a lightweight, cross-platform desktop workstation that imports field logs, converts alpha/beta measurements to structural orientations, and visualizes results on a stereonet with exportable summaries.

2) Success Criteria
- Import CSV/XLSX files with validation and clear error messages.
- Compute dip and dip direction (and optional strike) from alpha/beta using configurable conventions.
- Plot poles/planes on stereonet and export figures/data.
- Run on Linux/Windows/macOS from one codebase.
- Provide reproducible results via tests and sample datasets.

3) Scope
In scope (MVP):
- Data import and validation.
- Core geometry conversion engine.
- Stereonet plotting (poles first, planes optional).
- Basic desktop UI for load/calculate/plot/export.

Out of scope (initially):
- Database persistence.
- Advanced clustering/statistical interpretation.
- Full 3D geological modeling.

4) Architecture (separation of concerns)
A. UI layer (Qt: PySide6 preferred)
- File selection, table preview, run calculations, plot controls.
- Keep UI thin: no geology math in widgets.

B. Data layer (pandas)
- Read CSV/XLSX.
- Column mapping and validation.
- Missing value handling and type normalization.

C. Geology computation engine (numpy)
- Alpha/beta to dip+dip direction conversion.
- Strike derivation and vector transforms.
- Convention-aware configuration (reference line, angle conventions).

D. Visualization layer (matplotlib + mplstereonet)
- Pole scatter (MVP).
- Optional planes and density contours (later phase).

5) Proposed Repository Structure
- src/
  - app/
    - ui_main.py
    - controllers.py
    - stereonet_cli.py
  - domain/
    - structural_calculations.py
    - geometry.py
    - conventions.py
  - data/
    - io.py
    - validation.py
  - viz/
    - stereonet.py
  - config/
    - defaults.py
- tests/
  - test_geometry.py
  - test_validation.py
  - test_pipeline.py
  - test_stereonet.py
- sample_data/
- docs/

6) Data Contract (MVP)
Required input fields:
- hole_id
- depth
- alpha
- beta
- trend
- plunge

Optional fields:
- ref (aliases: reference_line, core_orientation_reference)
- lithology
- structure_type
- comments

Validation rules:
- Numeric parsing for angle/depth fields.
- Import aliases are normalized to canonical columns (`hole_id`, `depth`, `trend`, `plunge`, `alpha`, `beta`) and missing required canonical fields stop import with row-context errors.
- Angle bounds configurable by convention:
  - alpha: 0-90 (plane/core-axis angle)
  - beta: 0-360 (clockwise downhole angle from reference line)
- Row-level error reporting with row index and reason.

7) Milestones and Deliverables
Milestone 1: Computation CLI (Week 1) - Status: Completed
Completed deliverables:
- CLI script: import -> validate -> compute -> export CSV.
- Initial convention configuration.
- Unit tests for geometry conversion.
- Sample input/output and validation error artifacts.
- Geometry conversion refactored to match spreadsheet VBA ConvAlphaBeta logic.
Validation summary:
- Unit test suite passing (geometry, validation, and pipeline tests).
- Sample pipeline run completed successfully with invalid-row reporting.
- Workbook sample cases checked against Python outputs for dip/direction consistency.
Exit criteria: met
- Known test dataset produces expected dip/dip direction values.
- Invalid rows are reported without crashing.

Milestone 2: Stereonet Plotting (Week 2) - Status: Completed
Completed deliverables:
- Standalone plotting module implemented using `mplstereonet` (`src/viz/stereonet.py`).
- Pole plotting and figure export implemented for PNG and SVG.
- Basic filtering implemented for computed rows and optional CLI filters.
- Plotting CLI entrypoint implemented (`src/app/stereonet_cli.py`).
- Stereonet configuration defaults added in `src/config/defaults.py` with projection mode support (`equal-area` default, optional `equal-angle`) and polar-grid control.
- CLI options expose projection selection and grid toggling (`--projection`, `--polar-grid`, `--no-polar-grid`).
- Standalone stereonet example script added for quick local verification (`scripts/plot_stereonet_example.py`).
- Plotting dependencies and stereonet test coverage added.
Validation summary:
- Full unit test suite passing, including stereonet and stereonet CLI configuration tests (13/13).
- End-to-end stereonet export validated on computed sample data:
  - `sample_data/milestone2_poles.png`
  - `sample_data/milestone2_poles.svg`
- Script-based stereonet example export validated:
  - `sample_data/stereonet_example_from_prompt.png`
- Workbook-derived stereonet review artifacts generated, including row-labeled polar-grid exports:
  - `sample_data/alfabeta_stereonet_review_polar_grid.png`
  - `sample_data/alfabeta_stereonet_review_polar_grid.svg`
  - `sample_data/alfabeta_stereonet_review_polar_grid_labeled.png`
  - `sample_data/alfabeta_stereonet_review_polar_grid_labeled.svg`
- Synthetic reference-case reproducibility check completed:
  - stable filtered row count (`4`)
  - repeated exports succeeded with consistent artifact size
Exit criteria: met
- Correct pole placement verified on synthetic reference cases.
- Exported figures are reproducible.
- Deferred TODO (post-Milestone 2):
  - Improve plotting options (deferred for a later pass).

Milestone 3: Desktop UI (Week 3-4) - Status: Completed
Deliverables:
- PySide6 main window: Import, Calculate, Plot, Export.
- Table preview + output log + embedded matplotlib canvas (implemented).
- Plot action wiring with projection/polar-grid controls and controller guard behavior (implemented, with controller tests).
- Controller dependency-path/import-chain fixes completed (added `src/config/defaults.py`, `src/data/*`, `src/domain/*`, and controller test fixtures).
- Strict import alias handling implemented (required alias validation + canonical renaming) with Help dialog listing accepted column names.
- Optional reference-line aliases unified (`ref`, `reference_line`, `core_orientation_reference`) and reflected in computed outputs.
- Input loader supports CSV and ODS for import workflows.
- Error dialogs and non-blocking status updates.
Validation summary:
- Controller/UI action smoke tests completed successfully (`tests.test_controllers` passing).
- ConvAlphaBeta workbook-equivalent math restored in `src/domain/structural_calculations.py` with regression checks against workbook reference cases (`tests/test_structural_calculations.py`).
- Manual end-to-end workflow check completed successfully (`Import -> Calculate -> Plot -> Export`) with exported output artifacts generated.
- Full automated test suite currently passing (`unittest discover`: 18 tests).
Exit criteria: met
- End-to-end workflow succeeds from raw file to exported outputs.
- No geology logic in UI classes.

Milestone 4: Hardening & Packaging (Week 5)
Deliverables:
- Cross-platform path handling (pathlib).
- Packaging with PyInstaller.
- User guide and sample project files.
Exit criteria:
- App bundle launches on target OSes.
- Smoke tests pass on packaged build.

8) Technical Decisions
- UI: PySide6 (licensing and ecosystem fit).
- Math/data: numpy + pandas.
- Plotting: matplotlib + mplstereonet.
- Optional later: scipy for density/cluster analysis.

9) Quality Gates
Before each milestone is marked complete:
- Unit tests pass.
- Static checks/lint pass.
- Sample dataset run documented.
- One manual QA run from import to export completed.

10) Key Risks and Mitigations
Risk: Convention ambiguity (alpha/beta definitions vary).
Mitigation: Explicit convention profiles and visible selection in UI.

Risk: Dip direction vs strike confusion in outputs.
Mitigation: Store dip direction + dip as primary fields; compute strike as derived.

Risk: Core reference-frame inconsistency.
Mitigation: Require trend/plunge inputs and validate reference-line assumptions.

Risk: UI tightly coupled to domain logic.
Mitigation: Enforce domain API boundaries and test computation modules independently.

11) Immediate Next Actions (This Week)
1. Begin Milestone 4 hardening work with cross-platform path handling review.
2. Add packaging baseline (PyInstaller configuration and first build script pass).
3. Draft user guide updates for desktop workflow and troubleshooting.
4. Run packaged-build smoke checks on target environments.

12) Definition of Done (MVP)
MVP is done when a user can:
- Load a field log file.
- See validation feedback.
- Compute structural orientations with chosen convention.
- Plot poles on stereonet.
- Export both computed table and plotted figure reliably.
